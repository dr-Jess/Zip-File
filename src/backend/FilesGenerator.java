package backend;
import files.*;

public class FilesGenerator {
    /**
     * Generates a branching file system.
     * @param depth the depth of the tree
     * @param rootName the name of the root folder
     * @return the root file of the system
     */
    public static File generate(int depth, String rootName){
        if(depth<=1){
        }
    }
}
